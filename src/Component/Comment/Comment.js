import React from 'react';

const Comment = (props) => {
    const { postId, id, name, body, email } = props.comment;
    return (
        <div>
            
        </div>
    );
};

export default Comment;